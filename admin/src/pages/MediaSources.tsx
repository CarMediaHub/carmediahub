import { AppstoreOutlined, CloudServerOutlined, DatabaseOutlined, FolderOpenOutlined, KeyOutlined, LogoutOutlined, SafetyCertificateOutlined, TeamOutlined } from "@ant-design/icons";
import { ProCard, ProForm, ProFormSelect, ProFormText, ProLayout, ProTable } from "@ant-design/pro-components";
import { Button, message, Popconfirm, Space, Tag } from "antd";
import { useEffect, useState } from "react";
import { useAdminI18n } from "../i18n";
import { localizeAdminRoutes } from "../navigation";

type Source = { sourceHandle: string; name: string; binding: string; createdAt: string };
type Installation = { id: string; packageId: string; packageVersion: string; status: string };
type Credential = { id: string; name: string; kind: string; installationId: string };

const routes = [{ path: "/admin/overview", name: "Overview", icon: <AppstoreOutlined /> }, { path: "/admin/components", name: "Components", icon: <CloudServerOutlined /> }, { path: "/admin/media", name: "Media roots", icon: <FolderOpenOutlined /> }, { path: "/admin/media-sources", name: "Remote media sources", icon: <DatabaseOutlined /> }, { path: "/admin/keys", name: "Entry keys", icon: <KeyOutlined /> }, { path: "/admin/security", name: "Security", icon: <SafetyCertificateOutlined /> }, { path: "/admin/users", name: "Users", icon: <TeamOutlined /> }];

export default function MediaSources() {
  const { t } = useAdminI18n();
  const [sources, setSources] = useState<Source[]>([]);
  const [installations, setInstallations] = useState<Installation[]>([]);
  const [credentials, setCredentials] = useState<Credential[]>([]);
  const [selectedInstallation, setSelectedInstallation] = useState<string>();
  const refresh = () => { if (selectedInstallation === undefined) return; void fetch(`/api/media-sources?installationId=${encodeURIComponent(selectedInstallation)}`).then(async (response) => response.ok ? response.json() : { sources: [] }).then((data) => setSources(data.sources ?? [])); };
  useEffect(() => { void Promise.all([fetch("/api/plugins"), fetch("/api/credentials")]).then(async ([pluginsResponse, credentialsResponse]) => { const plugins = pluginsResponse.ok ? await pluginsResponse.json() : { installations: [] }; const stored = credentialsResponse.ok ? await credentialsResponse.json() : { credentials: [] }; const installed = (plugins.installations ?? []).filter((item: Installation) => item.status === "installed"); setInstallations(installed); setCredentials(stored.credentials ?? []); if (selectedInstallation === undefined && installed[0] !== undefined) setSelectedInstallation(installed[0].id); }); }, []);
  useEffect(() => { refresh(); }, [selectedInstallation]);
  return <ProLayout title="CarMediaHub" logo={false} route={{ routes: localizeAdminRoutes(routes, t) }} location={{ pathname: "/admin/media-sources" }} menuItemRender={(item, dom) => <a href={item.path}>{dom}</a>} actionsRender={() => [<Button key="logout" icon={<LogoutOutlined />} onClick={async () => { await fetch("/api/auth/logout", { method: "POST" }); window.location.href = "/admin/login"; }}>{t("nav.signOut")}</Button>]}> 
    <ProCard title={t("common.addRemoteSource")} style={{ margin: 24 }}><ProForm layout="inline" onFinish={async (values) => { const response = await fetch("/api/media-sources", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(values) }); if (!response.ok) { message.error(t("common.mediaSourceAddFailed")); return false; } message.success(t("common.mediaSourceAdded")); setSelectedInstallation(values.installationId); refresh(); return true; }}><ProFormSelect name="installationId" label={t("common.pluginInstallation")} fieldProps={{ onChange: setSelectedInstallation }} options={installations.map((item) => ({ label: `${item.packageId} (${item.id})`, value: item.id }))} rules={[{ required: true }]} /><ProFormText name="name" label={t("common.name")} rules={[{ required: true, max: 80 }]} /><ProFormText name="binding" label={t("common.binding")} rules={[{ required: true, pattern: /^[a-z][a-z0-9-]{0,63}$/u }]} /><ProFormText name="rootPath" label={t("common.remoteRootPath")} placeholder="/dav/" rules={[{ required: true }]} /><ProFormSelect name="credentialRef" label={t("common.credential")} allowClear options={credentials.filter((item) => item.installationId === selectedInstallation).map((item) => ({ label: `${item.name} (${item.kind})`, value: item.id }))} /></ProForm></ProCard>
    <ProCard title={t("common.remoteSources")} style={{ margin: 24 }}><ProTable<Source> rowKey="sourceHandle" search={false} options={{ reload: refresh }} dataSource={sources} columns={[{ title: t("common.name"), dataIndex: "name" }, { title: t("common.binding"), dataIndex: "binding" }, { title: t("common.handle"), dataIndex: "sourceHandle", render: (value) => <Tag>{String(value).slice(0, 18)}...</Tag> }, { title: t("common.added"), dataIndex: "createdAt" }, { title: t("common.action"), render: (_, row) => <Space><Button onClick={async () => { const response = await fetch(`/api/media-sources/${encodeURIComponent(row.sourceHandle)}/health?installationId=${encodeURIComponent(selectedInstallation ?? "")}`, { method: "POST" }); const result = response.ok ? await response.json() as { healthy: boolean; diagnostic: string } : undefined; if (result?.healthy) message.success(t("common.remoteHealthy")); else message.error(`${t("common.remoteUnavailable")} (${result?.diagnostic ?? "unknown"})`); }}>{t("common.check")}</Button><Popconfirm title={t("common.revokeSource")} onConfirm={async () => { const response = await fetch(`/api/media-sources/${encodeURIComponent(row.sourceHandle)}/revoke?installationId=${encodeURIComponent(selectedInstallation ?? "")}`, { method: "POST" }); if (!response.ok) { message.error(t("common.mediaSourceRevokeFailed")); return; } message.success(t("common.mediaSourceRevoked")); refresh(); }}><Button danger>{t("common.revoke")}</Button></Popconfirm></Space> }]} /></ProCard>
  </ProLayout>;
}
